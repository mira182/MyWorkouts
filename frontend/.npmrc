# Some third-party libs (e.g. @swimlane/ngx-charts, ngx-cookie) declare loose/old
# Angular peer ranges that npm's strict peer resolver rejects on Angular 21.
# They work fine at runtime, so relax peer resolution for install / npm ci.
legacy-peer-deps=true
